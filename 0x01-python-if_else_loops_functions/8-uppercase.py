#!/usr/bin/python3
def print_last_digit(number):
    for i in range(len(number)):
        if ord(number[i] >= 97 and ord(number[i]) <= 122:
                num = 32
        else:
                num = 0
        print("{:c}".format(ord(number[i] - num), end='')
    print()
